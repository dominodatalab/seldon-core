kind: NetworkPolicy
metadata:
  name: seldon-model-ingress
  namespace: seldon-regression-test
spec:
  ingress:
  - {}
  podSelector:
    matchLabels:
      seldon.io/model: "true"
  policyTypes:
  - Ingress
